# AMM-Docker (Automated Monolith Migration to Docker)

Инструментальное средство для автоматизированной контейнеризации монолитных Python-приложений. Проект разрабатывается в рамках выпускной квалификационной работы (ВКР).

## 📌 Описание проекта
Цель системы — упростить процесс миграции устаревших монолитных систем на микросервисную архитектуру путем автоматической генерации Docker-артефактов (Dockerfile, docker-compose.yaml) на основе анализа структуры исходного кода.

## 🛠 Текущий прогресс (Шаг 1: Автономизация)
На данный момент система успешно выполняет:
1. [cite_start]**Сканирование структуры**: Определение модулей (`users`, `products`, `orders`) и их зависимостей[cite: 15, 16].
2. **Миграция кода**: Физическое разделение кода монолита по отдельным директориям в `docker_out`.
3. **Генерация Entrypoints**: Создание уникальных `entrypoint.py` для каждого сервиса, что позволяет изолированно запускать Flask Blueprints.
4. **Инфраструктурная сборка**: Генерация индивидуальных `Dockerfile` и общего `docker-compose.yaml`.

## Архитектура решения
Система обрабатывает монолит, состоящий из следующих доменов:
* [cite_start]**Users**: Управление пользователями и расчет VIP-статуса[cite: 36, 40].
* [cite_start]**Products**: Каталог товаров и складской учет[cite: 41, 42].
* [cite_start]**Orders**: Управление транзакциями и связывание сущностей[cite: 45, 46].
* [cite_start]**Utils**: Общие системные утилиты[cite: 22, 58].

## Как запустить генерацию

### 1. Подготовка
Убедитесь, что установлены необходимые зависимости:
    ```bash
    pip install Flask Flask-SQLAlchemy Jinja2 customtkinter

### 2. Запуск через GUI
Запустите графический интерфейс для управления процессом миграции:
    ```bash
    python src/main_gui.py
В интерфейсе выберите путь к вашему монолиту и нажмите "Запустить миграцию".

### 3. Развертывание контейнеров
После завершения работы генератора, перейдите в папку с результатом и запустите систему:
    ```bash
    cd docker_out
    docker-compose up --build

План развития (Roadmap)
[x] Шаг 1: Изоляция точек входа и разделение кода (Готово).

[ ] Шаг 2: Настройка сетевого взаимодействия (HTTP-proxy для межсервисных вызовов).

[ ] Шаг 3: Декомпозиция базы данных (Разделение единого SQLite на независимые хранилища).