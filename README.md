# AMM-Docker (Automated Monolith Migration to Docker)

Инструментальное средство для автоматизированной контейнеризации монолитных Python-приложений. Проект разрабатывается в рамках выпускной квалификационной работы (ВКР).

## 📌 Описание проекта
Цель системы — упростить процесс миграции устаревших монолитных систем на микросервисную архитектуру путем автоматической генерации Docker-артефактов (Dockerfile, docker-compose.yaml) на основе анализа структуры исходного кода.

## 🛠 Текущий прогресс (Стадия «Рабочий проект»)

### Выполненные задачи:
- [x] **Создание тестового окружения** — разработан генератор `create_test_monolith.py` для имитации сложной структуры монолита.
- [x] **Анализ структуры и зависимостей** — модуль `scanner.py` автоматически находит пакеты, модули и файлы `requirements.txt`.
- [x] **Разбиение на функциональные модули** — система выделяет логические границы будущих сервисов.
- [x] **Автоматическое создание Dockerfile** — реализована динамическая генерация через шаблонизатор Jinja2.
- [x] **Генерация манифеста оркестрации** — автоматическое создание `docker-compose.yaml` для связи всех компонентов.
- [x] **Сборка пакета миграции** — автоматизирован сбор всех необходимых файлов в директорию `docker_out`.

### В планах:
- [ ] **Автоматическая сборка Docker-образов** — интеграция с Docker SDK.
- [ ] **Управление жизненным циклом** — запуск и остановка контейнеров через Python-менеджер.
- [ ] **Логирование процесса** — детальные отчеты о ходе миграции.

## Как запустить (текущая версия)

1. Установите зависимости:
-  Убедитесь, что у вас установлен Python 3.12+ и необходимые библиотеки
   ```bash
   pip install customtkinter jinja2

2. Сгенерируйте тестовый монолит:
    ```bash
    python3.14 create_test_monolith.py

3. Запустите генератор артефактов:
    ```Bash
    python3.14 main_gui.py

4. Процесс миграции
    1. В появившемся окне нажмите «ОБЗОР ПАПКИ».

    2. Выберите директорию вашего монолита (например, test_monolith_shop).

    3. Нажмите «ЗАПУСТИТЬ МИГРАЦИЮ».

    4. По завершении нажмите кнопку «ОТКРЫТЬ РЕЗУЛЬТАТ», чтобы увидеть сгенерированные Docker-контейнеры в папке docker_out/.


